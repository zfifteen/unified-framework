"""

 - read from the helical_embeddings.db
create table embeddings
(
    n INTEGER
        primary key,
    x REAL,
    y REAL,
    z REAL
);

  -- create an interactive web application that:
    - allows the user to interact with the 3D plot
    - allows the user to select a prime number
    - displays the prime's geodesic in 3D
    - displays the prime's geodesic in 2D

"""